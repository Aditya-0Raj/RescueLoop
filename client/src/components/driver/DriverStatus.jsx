export default function DriverStatus({ onDuty, onToggle }) { return <div className="card flex items-center justify-between p-5"><div><p className="eyebrow">Driver status</p><p className="mt-1 text-lg font-semibold">I’m on duty</p><p className="mt-1 text-xs text-slate-400">Only on-duty drivers receive rescue offers.</p></div><button onClick={onToggle} aria-pressed={onDuty} className={`relative h-7 w-12 rounded-full transition ${onDuty ? 'bg-forest' : 'bg-slate-200'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white transition ${onDuty ? 'left-6' : 'left-1'}`} /></button></div>; }
